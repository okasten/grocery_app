<h1><%= @product.name.capitalize %></h1>

Stores found:
<ul>
  <% @product.stores.each do |store| %>
    <li><%= link_to store.name, store_path(store) %>
    <%= store.address %></li>
  <% end %>
</ul>

Lists belongs to:
<ul>
<% @product.lists.each do |list| %>
  <li><%= link_to list.name, list_path(list) %></li>
<% end %>
</ul>
